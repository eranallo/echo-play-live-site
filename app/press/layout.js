import { pageMetadata } from '@/lib/public/seo.mjs'
export const metadata = pageMetadata({"title": "Band Kits & Press", "description": "Download Echo Play Live band kits, photos, logos and bios. Watch live performances and find booking contacts for all four bands.", "path": "/press"})

export default function PressLayout({ children }) {
  return children
}
